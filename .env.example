# Auto-injected by Vercel when you connect a Blob store to this project —
# you don't need to set this manually when deployed on Vercel.
# For local dev, run `vercel env pull .env.local` after linking the project,
# or copy the token from the Vercel dashboard (Storage -> your Blob store -> .env.local tab).
BLOB_READ_WRITE_TOKEN=
